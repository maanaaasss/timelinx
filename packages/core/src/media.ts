/**
 * @timelinx/core — Media Utilities
 *
 * Sub-path export for marker search.
 *
 * @example
 * ```ts
 * import { findMarkersByColor, findMarkersByLabel } from '@timelinx/core/media';
 * ```
 */

// ── Marker Search ──────────────────────────────────────────────────────────
export { findMarkersByColor, findMarkersByLabel } from './engine/marker-search';
