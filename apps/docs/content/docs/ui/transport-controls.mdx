---
title: TransportControls
description: Play, pause, and seek controls
---

import { AutoTypeTable } from 'fumadocs-typescript/ui';
import { generator } from '@/lib/generator';

The `TransportControls` component provides playback controls - play/pause toggle, frame stepping, and skip to start/end.

## Live Example

import { TransportControlsExample } from '@/components/examples/transport-controls-example.client';

<TransportControlsExample />

## Usage

```tsx
import { TransportControls, TimelineProvider } from '@timelinx/ui';

function App() {
  return (
    <TimelineProvider engine={engine}>
      <TransportControls />
    </TimelineProvider>
  );
}
```

## Props

<AutoTypeTable
  generator={generator}
  path="node_modules/@timelinx/ui/dist/index.d.ts"
  type="TransportControlsProps"
/>

## Context Requirement

`TransportControls` must be inside a `TimelineProvider`. It reads the playhead frame, playing state, and timeline metadata (fps, duration) from the engine context.
