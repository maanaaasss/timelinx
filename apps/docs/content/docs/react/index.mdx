---
title: React Integration
description: Hooks, providers, and tool routing with @timelinx/react
---

The `@timelinx/react` package provides React bindings for the timeline engine. It uses `useSyncExternalStore` for granular re-rendering — components only re-render when the specific state they read changes.

## Sections

- [Hooks](/docs/react/hooks) — `useEngine`, `useTimelineContext`, and all available hooks
- [Provider](/docs/react/provider) — setting up `TimelineProvider`

## Full API Reference

For exhaustive symbol-level detail, see the [API Reference](/docs/api).
