{
  "name": "timelinx",
  "version": "1.0.0",
  "description": "Browser-native timeline editor engine and UI",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20.0.0",
    "pnpm": ">=10.0.0"
  },
  "scripts": {
    "build": "pnpm -r --if-present build",
    "dev": "pnpm --filter @timelinx/docs dev",
    "test": "pnpm -r --if-present test",
    "lint": "pnpm -r --if-present lint",
    "typecheck": "pnpm -r --if-present typecheck",
    "editor:install": "pnpm --dir apps/editor --ignore-workspace install --frozen-lockfile",
    "editor:lint": "pnpm --dir apps/editor --ignore-workspace run lint",
    "editor:typecheck": "pnpm --dir apps/editor --ignore-workspace run typecheck",
    "editor:test": "pnpm --dir apps/editor --ignore-workspace run test",
    "editor:e2e": "pnpm --dir apps/editor --ignore-workspace run test:e2e",
    "editor:build": "pnpm --dir apps/editor --ignore-workspace run build",
    "editor:verify": "pnpm run editor:lint && pnpm run editor:typecheck && pnpm run editor:test && pnpm run editor:build",
    "format": "prettier --write .",
    "format:check": "prettier --check .",
    "ci": "pnpm lint && pnpm typecheck && pnpm build && pnpm test",
    "changeset": "changeset"
  },
  "keywords": [
    "timeline",
    "nle",
    "video-editor",
    "typescript",
    "browser"
  ],
  "author": "Timeline Contributors",
  "license": "MIT",
  "packageManager": "pnpm@10.28.2",
  "pnpm": {
    "overrides": {
      "@types/react": "^19.2.0",
      "@types/react-dom": "^19.2.0"
    },
    "onlyBuiltDependencies": [
      "esbuild"
    ]
  },
  "devDependencies": {
    "@changesets/changelog-github": "^1.0.0",
    "@changesets/cli": "^3.0.0",
    "@eslint/js": "^9.28.0",
    "@stryker-mutator/core": "^9.6.1",
    "@stryker-mutator/vitest-runner": "^9.6.1",
    "eslint": "^10.9.1",
    "globals": "^17.11.0",
    "playwright": "^1.62.1",
    "prettier": "^3.5.0",
    "typescript-eslint": "^8.33.0"
  }
}
